# Ensure all text files use LF line endings in the repo
* text=auto eol=lf

# Force specific files as binary (no conversion)
*.{png,jpg,jpeg,gif,ico,pdf,zip,rar} binary

# Ignore file permission changes (executable bits)
* -text
# or
* !executable

# Ignore HTML and CSS files for GitHub language statistics
# This ensures the repository shows as a JavaScript project

# Ignore HTML files (demo pages)
*.html linguist-detectable=false

# Ignore CSS files (styling for demos)
*.css linguist-detectable=false

# Ensure JavaScript files are detected
*.js linguist-detectable=true

# Optional: Mark specific files as documentation
README.md linguist-documentation=true
docs/** linguist-documentation=true

# Optional: Mark demo files as vendored (excluded from stats)
demo.html linguist-vendored=true
index.html linguist-vendored=true
demo/** linguist-vendored=true
examples/** linguist-vendored=true